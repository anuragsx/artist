<div class="row">
  <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <h1><%= params[:action] %> Vedio</h1>
  </div>
</div>
<div class="row topsp">
  <div class="loginform">
  <%# @url = params[:action]=='new' %>
  <%= form_for @video do |f| %>
      <% if video.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(video.errors.count, "error") %> prohibited this news from being saved:</h2>

          <ul>
          <% video.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>    
      <div class="col-xs-12 col-sm-12 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-30">          

        <div class="inputxt">Title :</div>
          <%= f.text_field :title, autofocus: true %>
        <br class="clear" />

        <div class="inputxt">URL :</div>
          <%= f.text_field :url, autofocus: true %>
        <br class="clear" />        
      
        <%= f.submit %>        
      </div>
    <% end -%>
  </div>
</div>