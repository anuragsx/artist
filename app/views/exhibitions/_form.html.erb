<%= form_for(exhibition) do |f| %>
  <% if exhibition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exhibition.errors.count, "error") %> prohibited this exhibition from being saved:</h2>

      <ul>
      <% exhibition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :exhibition_by %>
    <%= f.text_field :exhibition_by %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :from_data %>
    <%= f.text_field :from_data %>
  </div>

  <div class="field">
    <%= f.label :to_date %>
    <%= f.text_field :to_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
