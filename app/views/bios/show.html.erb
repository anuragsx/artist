<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @bio.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @bio.description %>
</p>

<%= link_to 'Edit', edit_bio_path(@bio) %> |
<%= link_to 'Back', bios_path %>
